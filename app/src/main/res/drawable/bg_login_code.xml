<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 背景填充颜色 -->
    <solid android:color="@color/base_blue" />
    <stroke
        android:width="0.5dp"
        android:color="@color/base_blue" /><!-- 描边，边框宽度、颜色 -->
    <corners android:radius="5dp" /><!-- 边角圆弧的半径 -->
    <padding
        android:bottom="2dp"
        android:left="10dp"
        android:right="10dp"
        android:top="2dp" /><!-- 四周留出来的空白 -->
</shape>